# Notebooks

Optional notebooks can be placed here for exploratory analysis, plots, and additional diagnostics. The main reproducible workflow should remain in `run_pipeline.py` and the `src/` modules so the instructor can run the project from the command line.
