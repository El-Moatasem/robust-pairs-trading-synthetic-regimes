# Notebooks

The production pipeline is implemented in Python modules so it can be tested and reproduced from the command line. Exploratory notebooks may be added here, but final results should be generated by `run_pipeline.py` to avoid notebook-state dependence.
